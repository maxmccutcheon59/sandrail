# Optional: OpenAI-compatible local API (secrets via env only — never commit real values)
# OPENAI_BASE_URL=http://127.0.0.1:11434/v1
# OPENAI_API_KEY=sk-local-not-a-real-secret
# OPENAI_MODEL=llama3.2
